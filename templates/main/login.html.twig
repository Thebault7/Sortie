
{% extends 'layout.html.twig' %}


{% block title %}
    <span class="contact1-form-title">
Connexion
</span>
{% endblock %}

{% block body %}

    {# app/Resources/views/security/login.html.twig #}
    {# ... you will probably extend your base template, like base.html.twig #}

    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <div class="col-md-12 col-lg-12 col-6 col-sm-6 col-xl-12">
    </div>

    <div class="container-contact1">

        <form action="{{ path('login') }}" method="post">

                <div class="p-2" style="width: 400px">

        <div class="d-inline-flex p-3">

            {% if error %}
                <div class="alert-error">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            <div class="wrap-input1 validate-input">
                <label for="login">Login (mail ou pseudo) :</label>
                <input type="text" id="name" name="_username" value="{{ last_username, {'attr': {'class': 'input1'}} }}"
                       style="width: 200px;">
                <span class="shadow-input1"></span>
            </div>
        </div>


                    <div class="wrap-input1 validate-input">
                <label for="mdp">Mot de passe :</label>
                <input type="password" id="name" name="_password"
                       style="width: 200px;">
                        <span class="shadow-input1"></span>
            </div>



                    {#
                        If you want to control the URL the user
                        is redirected to on success (more details below)
                        <input type="hidden" name="_target_path" value="/account"/>
                    #}

{#                    {{ form_start(RegistrationForm, {label_attr: {class: 'contact1-form align-items-sm-center justify-content-center'}}) }}#}

{#                   <div class="wrap-input1 validate-input">#}
{#                        {{ form_label(RegistrationForm.mail) }}#}
{#                       {{ form_widget(RegistrationForm.mail, {'attr': {'class': 'input1'} }) }}#}
{#                      {{ form_errors(RegistrationForm.mail) }}#}
{#                      <span class="shadow-input1"></span>#}
{#                   </div>#}

{#                    <div class="wrap-input1 validate-input">#}
{#                        {{ form_label(RegistrationForm.plainPassword) }}#}
{#                        {{ form_widget(RegistrationForm.plainPassword, {'attr': {'class': 'input1'} }) }}#}
{#                       {{ form_errors(RegistrationForm.plainPassword) }}#}
{#                      <span class="shadow-input1"></span>#}
{#                    </div>#}

{#                    {{ form_end(RegistrationForm) }}#}

                    <div class="container-contact1-form-btn" style="margin: 60px" >
                        <button class="contact1-form-btn" type="submit">Connexion</button>
                    </div>
                        </div>
                            <div style="float: right;width: 200px">
                                <a href="#">Mot de passe oublié</a>
                            </div>

    </form>

    </div>
{% endblock %}


